# ADR-0001: 三端合一的前端架构

系统采用单一 Next.js 应用通过路由区分三端（工程师/PM/管理），而非三个独立应用或微前端架构。

**理由**：
- 三端共享相同的 UI 组件库、SDK、业务逻辑
- 减少代码重复和维护成本
- 统一部署简化运维
- 团队规模不需要独立前端团队

**路由规划**：
- `/engineer/*` — 工程师端
- `/pm/*` — PM 端
- `/admin/*` — 管理端

**权限控制**：通过中间件检查用户角色，自动重定向到对应端。