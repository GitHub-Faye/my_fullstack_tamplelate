# 05 — 初始星点改为 100

**What to build:** 新注册工程师用户的 `current_starpoint` 初始值从 0 改为 100，对齐 PRD "初始星点 100 点"的规定。已有用户数据不受影响。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] User 模型 `current_starpoint` 默认值从 0 改为 100
- [ ] 新注册工程师仪表盘"当前星点"显示 100
- [ ] 已有用户数据不受影响（不自动迁移）